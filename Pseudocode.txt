// 1) Define game variables
- Set current player (🔴 or 🔵)
- Track moves in array
- Set red/blue counters to 3

// 2) Cache HTML elements
- Get squares, message, counters, and reset button

// 3) if game end
- Win message
- Call reset function

// 5) Set win (array)
- winning square sets (rows, cols, )

// 6) Handle square clicks
- In placing phase:
    - Place piece if spot is empty and player has pieces
- After place or move, check for win or switch player
- If all pieces placed:
    - Select and move if valid
    - After move check for win or switch player


// 7) Reset game
- On reset button click, restart game (play again)